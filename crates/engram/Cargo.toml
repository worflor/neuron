# Vendored from the user's Whisper Prism suite (the Engram Universal Trajectory Codec).
# Author: Woflo / MB. Patent pending. This is the user's own work, kept under its own
# license (LICENSE.md, copied from the Whisper project root) — distinct from Neuron's
# MIT/Apache dual license. Do NOT relicense; do NOT regress the math.
#
# KNOCKBACK uses this crate as the rhythm-familiar's brain: every exchange is an Engram
# block; the twin's knockback is `predict_all` run one beat past the player's motif.
[package]
name = "engram"
version = "0.1.0"
edition = "2024"
description = "The Whisper Engram Universal Trajectory Codec"
license-file = "LICENSE.md"
authors = ["Woflo / MB"]

[lib]
name = "engram"
crate-type = ["lib"]

[dependencies]
num-complex = "0.4"
rayon = "1.10"              # parallel pair processing

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
